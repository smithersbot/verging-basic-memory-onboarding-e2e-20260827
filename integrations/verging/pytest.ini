[pytest]
testpaths = tests
addopts = -ra
# The adapter is imported as integrations.verging, so the repository root has to
# be importable; src/ carries basic_memory itself.
pythonpath =
    ../..
    ../../src
timeout = 300
timeout_method = thread
