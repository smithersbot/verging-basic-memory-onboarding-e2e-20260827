"""Verging Memory CI product adapter for Basic Memory.

Verging Memory CI drives a memory product over a small HTTP contract. This
package is only that wire shape: every namespace it creates is a real Basic
Memory project, every memory it stores is a real markdown note, and every
recall is a real Basic Memory search. There is no adapter-side store.
"""
